import React from "react";
import ReactDOM from "react-dom";
import App from "./App.js";
ReactDOM.render(
  <App />,
  document.getElementById("root")
  // rendering a component named App, created next, and is rendered at the DOM element with the ID root.
);

// ReactDOM.render function tells React what to render and where to render it.
